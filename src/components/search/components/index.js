import FilterHeader from './FilterHeader'
import FilterSort from './FilterSort'
import FilterClazz from './FilterClazz'
import FilterBasic from './FilterBasic'
import FilterData from './FilterData'
import FilterDate from './FilterDate'
import FilterTags from './FilterTags'

export { FilterHeader, FilterSort, FilterClazz, FilterBasic, FilterData, FilterDate, FilterTags }

// const files = require.context('./', false, /\.vue$/)
// const modules = {}

// files.keys().forEach(key => {
//   modules[key.replace(/(\.\/|\.vue)/g, '')] = files(key).default
// })

// export default modules
